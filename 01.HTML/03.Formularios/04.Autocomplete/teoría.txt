El atributo "autocomplete" se utiliza para controlar si el navegador 
debe completar automáticamente los campos de un formulario con datos previamente ingresados por el usuario.
Este atributo puede tener los valores "on" u "off" (para permitir o no que el navegador autocomplete).

El atributo "require" se utiliza para hacer que un campo de un formulario sea obligatorio, es decir,
que el usuario deba ingresar un valor antes de poder enviar el formulario.
Este atributo puede agregarse a cualquier elemento de formulario, como input, select y textarea.
no recibe valores, es un atributo de solo nombre.